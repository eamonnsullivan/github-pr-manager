(ns eamonnsullivan.github-pr-manager.test
  (:require [clojure.test :refer :all]
            [eamonnsullivan.github-pr-manager :refer :sut]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
